<?xml version="1.0" encoding="utf-8"?>
<!--
  Dev-only cleartext (HTTP, not HTTPS) exception for the local Python RAG
  voice-assistant service (sahulat-rag), which runs on a developer's machine
  and isn't behind TLS. The Rails backend is unaffected - it's HTTPS and
  covered by the platform default (base-config cleartextTrafficPermitted
  stays false for everything not listed here).
-->
<network-security-config>
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">localhost</domain>
        <domain includeSubdomains="false">127.0.0.1</domain>
        <domain includeSubdomains="false">192.168.43.249</domain>
    </domain-config>
</network-security-config>
